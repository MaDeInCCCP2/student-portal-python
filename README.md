# 🎓 Личный кабинет студента (Student Portal)

Простая информационная система для управления данными студентов.
Реализована на чистом **Python** (без использования тяжелых фреймворков) с использованием встроенной библиотеки `http.server` и базы данных **SQLite**.

## 🚀 Функциональность

*   **Автоматическая инициализация:** При первом запуске скрипт сам создает базу данных и таблицы.
*   **Веб-интерфейс:** Отображение приветственной страницы и статистики через браузер.
*   **Работа с БД:** Чтение количества зарегистрированных студентов из SQLite.
*   **MVC Архитектура:** Разделение логики (main.py), данных (models) и представления (templates).

## 🛠️ Технологии

*   **Язык:** Python 3.10+
*   **База данных:** SQLite3
*   **Frontend:** HTML5, CSS (встроенный)
*   **Сервер:** http.server (Native Python lib)

## 📦 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```
    git clone https://github.com/MaDeInCCCP2/student-portal-python.git
    cd student-portal-python
    ```

2.  **Запустите приложение:**
    ```
    python main.py
    ```

3.  **Откройте в браузере:**
    Перейдите по адресу: [http://localhost:8000](http://localhost:8000)

## 📂 Структура проекта

*   `main.py` — Точка входа, запуск веб-сервера.
*   `db_init.py` — Скрипт создания таблиц и добавления тестовых данных.
*   `models/` — Python-классы (сущность Student).
*   `templates/` — HTML-шаблоны страниц.
*   `database/` — Файл базы данных (появляется после запуска).
